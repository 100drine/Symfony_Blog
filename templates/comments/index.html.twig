{% for comment in comments %}
    <div class="card">
        <div class="card-body">
            <h4>{{ comment.author }}</h4>
            <p>{{ comment.content }}</p>
            <p>{{ comment.createdAt }}</p>
        </div>
    </div>
{% endfor %}
